{
  "Config": [
    {
      "debug_mode": "False",              // stores/reads cache'd .pkl files
      "TEMP_CACHE": "temp_cache"         // the folder where the cache's are stored
    } 
  ],
  "Training": {
    // Training specific directories
    "MODEL_PARAMETERS": "machine_learning/parameters.json",
    "RESULTS_ROOT": "Results",
    "DEPLOYED_MODELS": "Results/deployed_models",

    "observation_end": "2026/03/31",      // The last available entry date for the research study (YYYY/MM/DD)
    "target_feature": "dtp_bracket",
    "test_size": "0.30"
  }
}